# better-auth declara decenas de peerDependencies opcionales (prisma, pg, mysql2,
# vue, svelte...). legacy-peer-deps evita que npm intente instalarlas y evita
# conflictos de peers benignos. No afecta a las dependencias reales del proyecto.
legacy-peer-deps=true
save-exact=false
